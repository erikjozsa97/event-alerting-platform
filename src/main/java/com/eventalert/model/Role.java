package com.eventalert.model;

/**
 * The two account permission levels: a regular self-service user, or an admin
 * with access to the read-only admin API.
 */
public enum Role {
    USER,
    ADMIN
}
